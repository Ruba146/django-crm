generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model activities {
  id               String?
  entity_type      String?
  entity_id        String?
  activity_type_id String?
  direction        String?
  duration_seconds Int?
  outcome          String?
  body             String?
  user_id          String?
  occurred_at      String?
  created_at       String?
  updated_at       String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model activity_mentions {
  id                String?
  activity_id       String?
  mentioned_user_id String?
  created_at        String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model activity_types {
  id          String?
  label       String?
  color       String?
  sort_order  Int?
  is_archived Int?
  created_at  String?
  updated_at  String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model assignment_policies {
  id                 String?
  policy             String?
  round_robin_pool   String?
  round_robin_cursor Int?
  updated_at         String?
  updated_by_id      String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model audit_log {
  id          String?
  actor_id    String?
  actor_ip    String?
  entity_type String?
  entity_id   String?
  action      String?
  before      String?
  after       String?
  created_at  String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model bot_rule_settings {
  id            String?
  rules_version String?
  threshold     Int?
  weights       String?
  updated_by    String?
  updated_at    String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model competitors {
  id          String?
  label       String?
  color       String?
  sort_order  Int?
  is_archived Int?
  created_at  String?
  updated_at  String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model contacts {
  id                String?
  establishment_id  String?
  full_name         String?
  role              String?
  phone             String?
  email             String?
  preferred_channel String?
  notes             String?
  deleted_at        String?
  created_at        String?
  updated_at        String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model deal_competitors {
  id              String?
  deal_id         String?
  competitor_id   String?
  is_primary      Int?
  their_advantage String?
  our_advantage   String?
  decision_driver String?
  notes           String?
  logged_by_id    String?
  logged_at       String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model deals {
  id                      String?
  lead_id                 String?
  establishment_id        String?
  stage_id                String?
  owner_id                String?
  name                    String?
  notes                   String?
  deleted_at              String?
  created_at              String?
  updated_at              String?
  expected_value_minor    Int?
  won_value_minor         Int?
  currency_code           String?
  mrr_minor               Int?
  contract_length_months  Int?
  seat_count              Int?
  probability_pct         Int?
  target_close_date       String?
  actual_close_date       String?
  contract_end_date       String?
  discount_requested_pct  Int?
  discount_approved_pct   Int?
  discount_approved_by_id String?
  discount_status         String?
  lost_reason_id          String?
  custom_fields           String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model discount_events {
  id              String?
  deal_id         String?
  requested_by_id String?
  approved_by_id  String?
  requested_pct   Int?
  approved_pct    Int?
  action          String?
  notes           String?
  created_at      String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model duplicate_candidates {
  id                String?
  primary_lead_id   String?
  candidate_lead_id String?
  match_reason      String?
  status            String?
  reviewed_by_id    String?
  reviewed_at       String?
  created_at        String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model email_log {
  id              String?
  recipient_id    String?
  recipient_email String?
  event_type      String?
  event_key       String?
  subject         String?
  status          String?
  smtp_message_id String?
  error           String?
  sent_at         String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model erp_tenant_overrides {
  erp_tenant_id  Int?
  classification String?
  notes          String?
  classified_by  String?
  classified_at  String?
  updated_at     String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model establishments {
  id                             String?
  name                           String?
  commercial_registration_number String?
  tax_number                     String?
  industry_id                    String?
  city                           String?
  address                        String?
  num_branches                   Int?
  has_warehouse                  Int?
  num_pos                        Int?
  current_system                 String?
  customer_requirements          String?
  expected_value_minor           Int?
  notes                          String?
  deleted_at                     String?
  created_at                     String?
  updated_at                     String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model industries {
  id          String?
  label       String?
  color       String?
  sort_order  Int?
  is_archived Int?
  created_at  String?
  updated_at  String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ingestion_runs {
  id               String?
  source_config_id String?
  status           String?
  started_at       String?
  finished_at      String?
  rows_fetched     Int?
  rows_inserted    Int?
  rows_updated     Int?
  rows_errored     Int?
  error_details    String?
  triggered_by     String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model invited_emails {
  id          String?
  email       String?
  role        String?
  invited_by  String?
  created_at  String?
  accepted_at String?
  revoked_at  String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model junk_reasons {
  id          String?
  label       String?
  color       String?
  sort_order  Int?
  is_archived Int?
  created_at  String?
  updated_at  String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model lead_touchpoints {
  id               String?
  lead_id          String?
  source_id        String?
  source_record_id String?
  campaign_id      String?
  ad_id            String?
  form_id          String?
  occurred_at      String?
  raw_payload      String?
  created_at       String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model leads {
  id                   String?
  full_name            String?
  normalized_phone     String?
  normalized_email     String?
  stage_id             String?
  primary_source_id    String?
  establishment_id     String?
  owner_id             String?
  referrer_contact_id  String?
  referrer_employee_id String?
  junk_reason_id       String?
  merged_into_id       String?
  custom_fields        String?
  notes                String?
  deleted_at           String?
  created_at           String?
  updated_at           String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model lost_reasons {
  id          String?
  label       String?
  color       String?
  sort_order  Int?
  is_archived Int?
  created_at  String?
  updated_at  String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model notes {
  id                 String?
  entity_type        String?
  entity_id          String?
  body               String?
  author_id          String?
  mentioned_user_ids String?
  created_at         String?
  updated_at         String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model notification_buffer {
  id              String?
  recipient_id    String?
  entity_type     String?
  entity_id       String?
  first_queued_at String?
  last_event_at   String?
  events          String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model notification_preferences {
  user_id              String?
  assignment_enabled   Int?
  activity_enabled     Int?
  stage_enabled        Int?
  task_enabled         Int?
  mention_enabled      Int?
  quiet_hours_start    String?
  quiet_hours_end      String?
  quiet_hours_timezone String?
  updated_at           String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pipeline_stages {
  id              String?
  pipeline        String?
  label           String?
  color           String?
  sort_order      Int?
  is_terminal     Int?
  terminal_type   String?
  requires_reason Int?
  is_archived     Int?
  created_at      String?
  updated_at      String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model saved_views {
  id         String?
  page       String?
  name       String?
  slug       String?
  filters    String?
  owner_id   String?
  created_by String?
  created_at String?
  updated_at String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model source_configs {
  id            String?
  source_id     String?
  name          String?
  adapter_key   String?
  config        String?
  is_active     Int?
  schedule_cron String?
  created_at    String?
  updated_at    String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model sources {
  id          String?
  label       String?
  color       String?
  sort_order  Int?
  is_archived Int?
  created_at  String?
  updated_at  String?
  adapter_key String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tags {
  id          String?
  label       String?
  color       String?
  sort_order  Int?
  is_archived Int?
  created_at  String?
  updated_at  String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model task_type_options {
  id           String?
  task_type_id String?
  label        String?
  color        String?
  sort_order   Int?
  is_default   Int?
  is_archived  Int?
  created_at   String?
  updated_at   String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model task_types {
  id          String?
  label       String?
  color       String?
  sort_order  Int?
  is_archived Int?
  created_at  String?
  updated_at  String?
  has_options Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tasks {
  id                   String?
  entity_type          String?
  entity_id            String?
  task_type_id         String?
  task_type_option_id  String?
  title                String?
  description          String?
  mode                 String?
  location             String?
  meeting_url          String?
  assignee_id          String?
  due_at               String?
  completed_at         String?
  outcome              String?
  google_event_id      String?
  google_meet_url      String?
  calendar_sync_status String?
  calendar_sync_error  String?
  created_at           String?
  updated_at           String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ticket_attachments {
  id           String?
  ticket_id    String?
  comment_id   String?
  filename     String?
  content_type String?
  size_bytes   Int?
  data         Bytes?
  uploaded_by  String?
  created_at   String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ticket_comments {
  id         String?
  ticket_id  String?
  author_id  String?
  body       String?
  created_at String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tickets {
  id                 String?
  type               String?
  title              String?
  description        String?
  priority           String?
  status             String?
  end_date           String?
  reporter_tenant_id Int?
  reporter_user_id   String?
  owner_id           String?
  closed_at          String?
  created_by         String?
  created_at         String?
  updated_at         String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model users {
  id                      String?
  email                   String?
  name                    String?
  email_verified          String?
  image                   String?
  google_email            String?
  google_user_id          String?
  password_hash           String?
  roles                   String?
  locale                  String?
  google_calendar_enabled Int?
  is_active               Int?
  created_at              String?
  updated_at              String?

  @@ignore
}
