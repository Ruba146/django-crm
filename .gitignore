# Stale duplicate database at repo root — the app uses crm_project/crm.db.
# Anchored with a leading slash so it only matches the root copy and never the
# tracked crm_project/crm.db.
/crm.db

# Python bytecode
__pycache__/
*.py[cod]
